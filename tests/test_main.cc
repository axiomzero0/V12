// =============================================================================
// tests/test_main.cc
// =============================================================================
// Test runner main. Builds with all test files and runs them.

#include <cstdio>

#include "tests/test-framework.h"

int main(int argc, char** argv) {
    (void)argc;
    (void)argv;
    return RUN_ALL_TESTS();
}
